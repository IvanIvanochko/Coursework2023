@model List<Coursework.Models.OrderInfoModel>

<!DOCTYPE html>
<html>
<head>
    <link rel="stylesheet" href="~/css/Driver/DriverOrders.css" asp-append-version="true" />
</head>
<body>
    <div class="container">

    <h1>Мої замовлення</h1>

    @if (Model.Count > 0)
    {
        <table>
            <tr>
                <th>ID</th>
                <th>Дата</th>
                <th>Від міста</th>
                <th>До міста</th>
                <th>Вантажопідйомність (кг)</th>
            </tr>
            @foreach (var order in Model)
            {
                <tr>
                    <td>@order.ID</td>
                    <td>@order.Date</td>
                    <td>@order.FromCity.City, @order.FromCity.Address</td>
                    <td>@order.ToCity.City, @order.ToCity.Address</td>
                    <td>@order.Capacity_kg</td>
                </tr>
            }
        </table>
    }
    else
    {
        <p>Замовлення не знайдені.</p>
    }
    </div>
</body>
</html>


@*<h1>My Orders</h1>

@if (Model.Count > 0)
{
    <table>
        <tr>
            <th>ID</th>
            <th>Date</th>
            <th>From City</th>
            <th>To City</th>
            <th>Capacity (kg)</th>
        </tr>
        @foreach (var order in Model)
        {
            <tr>
                <td>@order.ID</td>
                <td>@order.Date</td>
                <td>@order.FromCity.City, @order.FromCity.Address</td>
                <td>@order.ToCity.City, @order.ToCity.Address</td>
                <td>@order.Capacity_kg</td>
            </tr>
        }
    </table>
}
else
{
    <p>No orders found.</p>
}*@
